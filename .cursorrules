# AI 助手全局编码规范指令

在后续的所有代码编写、生成与重构中，必须严格遵循以下【高复用、模块化、组件化】的规范：

## 1. 核心结构要求
- 禁止把所有功能写在一个文件里，必须按功能拆分独立文件/文件夹
- 每个文件夹对应一个独立小功能/页面，结构清晰、职责单一
- 公共逻辑、通用组件、工具函数必须单独封装，禁止重复代码

## 2. 封装规范
- 通用组件：独立封装，支持传参/配置，可在任意文件直接调用
- 工具函数：单独抽离为公共模块，全局复用
- 业务逻辑：和UI/界面代码完全分离，解耦开发

## 3. 目录结构要求
- 必须给出清晰的项目/文件目录树
- 文件夹命名：小写下划线/短横线，见名知意
- 文件命名：语义化，明确表达功能
- 主入口文件 + 拆分的子模块文件分开

## 4. 使用规范
- 每个封装好的组件/模块，必须给出调用示例
- 导入方式简洁，无需重复编写逻辑
- 代码注释清晰，方便后续维护和二次复用

## 5. 最终输出要求
- 先输出【目录结构树】
- 再分文件输出完整代码
- 最后输出【使用示例】
